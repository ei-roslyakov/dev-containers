{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "Group Docker base image updates into one PR",
      "matchManagers": ["dockerfile"],
      "groupName": "docker-images"
    },
    {
      "description": "Pin the Ubuntu base image to a digest so security refreshes of the same release still come through",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ubuntu"],
      "pinDigests": true
    },
    {
      "description": "Each directory pins its own Ubuntu release — never bump the version, only the digest",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ubuntu"],
      "matchUpdateTypes": ["major", "minor"],
      "enabled": false
    }
  ]
}
